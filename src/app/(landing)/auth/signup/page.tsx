import Signup from "@/sections/landing/auth/signup";
import type { Metadata } from "next";

export default function SignupPage() {
  return <Signup />;
}

export const metadata: Metadata = {
  title: "Sign up | RiseFinance",
};
