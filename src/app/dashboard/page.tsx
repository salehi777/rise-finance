import Dashboard from "@/sections/dashboard/dashboard";
import type { Metadata } from "next";

export default function DashboardPage() {
  return <Dashboard />;
}

export const metadata: Metadata = {
  title: "Dashboard | RiseFinance",
};
